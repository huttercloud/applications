apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "5.8.32"

dependencies:
- name: jenkins
  version: "5.8.32"
  repository: "https://charts.jenkins.io"
