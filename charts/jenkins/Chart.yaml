apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "4.11.2"

dependencies:
- name: jenkins
  version: "4.11.2"
  repository: "https://charts.jenkins.io"
