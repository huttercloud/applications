apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "4.3.30"

dependencies:
- name: jenkins
  version: "4.3.30"
  repository: "https://charts.jenkins.io"
