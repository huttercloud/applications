apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "5.7.21"

dependencies:
- name: jenkins
  version: "5.7.21"
  repository: "https://charts.jenkins.io"
