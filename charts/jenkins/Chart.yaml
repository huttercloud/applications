apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "5.0.13"

dependencies:
- name: jenkins
  version: "5.0.13"
  repository: "https://charts.jenkins.io"
