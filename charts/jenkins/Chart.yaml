apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "4.12.1"

dependencies:
- name: jenkins
  version: "4.12.1"
  repository: "https://charts.jenkins.io"
