apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "5.0.17"

dependencies:
- name: jenkins
  version: "5.0.17"
  repository: "https://charts.jenkins.io"
