apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "5.7.26"

dependencies:
- name: jenkins
  version: "5.7.26"
  repository: "https://charts.jenkins.io"
