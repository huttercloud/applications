apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "5.1.12"

dependencies:
- name: jenkins
  version: "5.1.12"
  repository: "https://charts.jenkins.io"
