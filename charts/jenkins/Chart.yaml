apiVersion: v2
name: jenkins
description: install and configure jenkins
type: application
version: "5.7.14"

dependencies:
- name: jenkins
  version: "5.7.14"
  repository: "https://charts.jenkins.io"
